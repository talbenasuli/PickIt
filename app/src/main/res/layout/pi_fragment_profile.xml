<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="15dp"
            android:src="@color/colorGray"
            android:id="@+id/profilePictureImageView" />

        <ImageButton
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignBottom="@+id/profilePictureImageView"
            android:layout_alignLeft="@+id/profilePictureImageView"
            android:layout_marginLeft="150dp"
            android:id="@+id/settingsImageButton"
            android:src="@color/colorAccent"/>

        <ImageButton
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignBottom="@+id/profilePictureImageView"
            android:layout_alignRight="@+id/profilePictureImageView"
            android:layout_marginRight="150dp"
            android:id="@+id/openCameraImageButton"
            android:src="@color/colorAccent" />

    </RelativeLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My PickIts"
        android:textSize="30dp"
        android:textColor="@color/colorBlack"/>

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_marginTop="5dp"
        android:background="@color/colorPrimary"
        android:id="@+id/myPickitsRecyclerView"
        android:paddingLeft="@dimen/activity_horizontal_margin"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My PickIts"
        android:textSize="30dp"
        android:textColor="@color/colorBlack"/>

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:background="@color/colorPrimary"
        android:layout_marginTop="5dp"
        android:id="@+id/placesVisitedRecyclerView"
        android:paddingLeft="@dimen/activity_horizontal_margin"/>

</LinearLayout>